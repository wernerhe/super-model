---
description: Run super-delete-branch - safety-checked branch deletion (value-check first, then delete with one confirmation)
---

Use the super-delete-branch skill to verify a branch has no unique value (no merged commits left behind, no uncommitted work, no parked notes) before deleting. One confirmation prompt then the branch is removed.
