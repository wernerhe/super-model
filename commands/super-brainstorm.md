---
description: Run super-brainstorm - turn an idea into a committed spec, an implementation plan, and a clean handoff to super-execute
---

Use the super-brainstorm skill to walk the user from an idea through clarifying questions, approach proposal, sectioned design, spec writing (with user review gate), plan writing, and handoff to super-execute. If the user passed a preset name as an argument (e.g., `full`, `fast`, `spec-only`), use that preset; otherwise use the default (`full`).
