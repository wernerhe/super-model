---
description: Run super-mcp-builder - discover, install, alter, or build MCP servers for this project
---

Use the super-mcp-builder skill to manage MCP servers. The skill reads the user's MCP config and the project `.mcp.json` to decide what to discover, install, or alter. Super-Model does NOT bundle any MCP server source code - all installs are from upstream sources the user picks.
